#!/bin/bash
################################################################################
# บทที่ 1: สคริปต์สร้างโครงสร้างโปรเจกต์ Audio DIY Mall
#
# ## ปรัชญาการออกแบบ (Design Philosophy) ##
# 1. Separation of Concerns: แยกส่วนของโค้ดตามหน้าที่อย่างชัดเจน
#    - /app:      จัดการเรื่อง Routing, หน้าเว็บ และ Backend API
#    - /components: จัดการเรื่องชิ้นส่วน UI ที่นำกลับมาใช้ใหม่ได้ (หน้าตาเว็บ)
#    - /lib:      จัดการเรื่อง Logic หลักของแอป (สมองของแอป)
#
# 2. Role-Based Routing: ใช้ URL ในการแบ่งแยกบทบาทของผู้ใช้งานอย่างชัดเจน
#    - /admin/**:  สำหรับผู้ดูแลระบบ
#    - /vendor/**: สำหรับผู้ขาย
#    - /account/**: สำหรับลูกค้าที่ login แล้ว
#
# 3. Feature-Based Components: จัดกลุ่ม Components ตามฟีเจอร์ที่มันทำงานด้วย
#    เพื่อให้ง่ายต่อการค้นหาและดูแลรักษา
################################################################################

echo "🚀 บทที่ 1: เริ่มสร้างโครงสร้างสถาปัตยกรรมโปรเจกต์ Audio DIY Mall..."
echo "---------------------------------------------------------------------"

# ==============================================================================
# ส่วนที่ 1: /app - หัวใจหลักของเว็บ (Routing, Pages, API)
#
# เราใช้ File-based Routing ของ Next.js โดยชื่อโฟลเดอร์จะกลายเป็น URL
# เพื่อแยกส่วนของ Admin และ Vendor อย่างชัดเจนและป้องกัน URL ชนกัน
# ==============================================================================
echo "⚙️  1. กำลังสร้างไดเรกทอรี /app สำหรับ Routing และ API..."

# --- (public) Route Group ---
# เราใช้ (public) เพื่อ "จัดกลุ่ม" หน้าเว็บที่คนทั่วไปเข้าถึงได้
# ข้อดี: ทำให้โฟลเดอร์ /app ไม่รก และที่สำคัญคือ "วงเล็บ ()" จะบอก Next.js ว่า
# ไม่ต้องนำชื่อ 'public' ไปสร้างเป็น URL ทำให้ URL ของเราสะอาด (เช่น /products/123)
mkdir -p app/'(public)'/products/'[id]'
mkdir -p app/'(public)'/stores/'[slug]'

# --- Vendor Section (URL จะขึ้นต้นด้วย /vendor) ---
# โฟลเดอร์นี้ "ไม่มีวงเล็บ" เพราะเราต้องการให้ URL แสดงอย่างชัดเจนว่าเป็นส่วนของผู้ขาย
mkdir -p app/vendor/dashboard
mkdir -p app/vendor/products/new
mkdir -p app/vendor/settings

# --- Admin Section (URL จะขึ้นต้นด้วย /admin) ---
# เช่นเดียวกับ Vendor การไม่มีวงเล็บทำให้ URL ชัดเจนว่าเป็นส่วนของผู้ดูแลระบบ
mkdir -p app/admin/dashboard
mkdir -p app/admin/stores
mkdir -p app/admin/users

# --- Customer Account Section (URL จะขึ้นต้นด้วย /account) ---
mkdir -p app/account/orders
mkdir -p app/account/profile

# --- API Routes ---
# ส่วน Backend ของเรา จัดกลุ่มตามประเภทของข้อมูล
mkdir -p app/api/products
mkdir -p app/api/stores
mkdir -p app/api/auth

# ==============================================================================
# ส่วนที่ 2: /components - กล่องเครื่องมือสร้าง UI (Lego Bricks)
#
# เราแบ่ง Components ตามหน้าที่และความซับซ้อน
# - ui: ชิ้นส่วนพื้นฐานที่สุด นำไปใช้ได้ทุกที่ (ปุ่ม, การ์ด)
# - features: ชิ้นส่วนที่ประกอบขึ้นมาเพื่อฟีเจอร์เฉพาะทาง
# - layout: โครงสร้างหลักของเว็บ (Navbar, Footer)
# ==============================================================================
echo "🎨 2. กำลังสร้างไดเรกทอรี /components สำหรับ UI..."
mkdir -p components/ui
mkdir -p components/layout
mkdir -p components/features/product   # สำหรับ UI แสดงผลสินค้าฝั่งลูกค้า
mkdir -p components/features/store    # สำหรับ UI แสดงผลร้านค้าฝั่งลูกค้า
mkdir -p components/features/vendor   # สำหรับ UI ในหน้า Dashboard ของผู้ขาย
mkdir -p components/features/admin    # สำหรับ UI ในหน้า Dashboard ของแอดมิน
mkdir -p components/features/account  # สำหรับ UI ในหน้าข้อมูลของลูกค้า

# ==============================================================================
# ส่วนที่ 3: /lib - สมองของแอป และสะพานสู่ React Native
#
# ## React Native Readiness ##
# ทั้งไดเรกทอรี /lib นี้ถูกออกแบบมาให้ "ไม่ขึ้นกับแพลตฟอร์ม" (Platform-agnostic)
# หมายความว่า Logic, Types, และฟังก์ชันเรียก API ทั้งหมดในนี้
# สามารถคัดลอกไปใช้ร่วมกับแอปพลิเคชัน React Native ในอนาคตได้ทันที
# ==============================================================================
echo "🧠 3. กำลังสร้างไดเรกทอรี /lib สำหรับ Core Logic (พร้อมสำหรับ React Native)..."
mkdir -p lib/api      # ฟังก์ชันสำหรับ "เรียก" API จากฝั่ง Client
mkdir -p lib/hooks    # Custom Hooks ที่เก็บ Business Logic
mkdir -p lib/utils    # ฟังก์ชันช่วยเหลือทั่วไป
mkdir -p lib/validators # Schema สำหรับตรวจสอบความถูกต้องของข้อมูล
touch lib/types.ts    # พิมพ์เขียวข้อมูล (TypeScript Types)

# ==============================================================================
# ส่วนที่ 4: /public - ที่เก็บทรัพย์สินสาธารณะ (Static Assets)
# ==============================================================================
echo "🖼️  4. กำลังสร้างไดเรกทอรี /public สำหรับรูปภาพและไฟล์สาธารณะ..."
mkdir -p public/images
mkdir -p public/logos

# ==============================================================================
# ส่วนที่ 5: สร้างไฟล์เริ่มต้น (Placeholder Files)
# ==============================================================================
echo "📄 5. กำลังสร้างไฟล์เริ่มต้นในแต่ละไดเรกทอรี..."

# --- App Files ---
touch app/layout.jsx
touch app/globals.css
touch app/'(public)'/page.jsx # Homepage
touch app/'(public)'/products/'[id]'/page.jsx
touch app/'(public)'/stores/'[slug]'/page.jsx
touch app/'(public)'/not-found.jsx
touch app/vendor/dashboard/page.jsx
touch app/vendor/products/page.jsx
touch app/vendor/products/new/page.jsx
touch app/vendor/settings/page.jsx
touch app/admin/dashboard/page.jsx
touch app/api/products/route.js
touch app/api/stores/route.js

# --- Component Files ---
touch components/ui/Button.jsx
touch components/ui/Card.jsx
touch components/ui/Input.jsx
touch components/layout/Navbar.jsx
touch components/layout/Footer.jsx
touch components/features/product/ProductCard.jsx
touch components/features/vendor/ProductForm.jsx
touch components/features/admin/AdminStoreTable.jsx

# --- Config & Other Files ---
touch .env.local
touch .gitignore
touch README.md
touch jsconfig.json
touch tailwind.config.js
touch postcss.config.js
touch package.json

echo "---------------------------------------------------------------------"
echo "✅ บทที่ 1 เสร็จสมบูรณ์! โครงสร้างโปรเจกต์ถูกสร้างขึ้นอย่างถูกต้อง"
echo "   ตอนนี้เรามีรากฐานที่แข็งแรง พร้อมสำหรับบทต่อไปแล้วครับ ✨"