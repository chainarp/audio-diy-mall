บทที่ 3: React และ Next.js - Foundation to Production
📚 สารบัญ

เครื่องมือพื้นฐาน (Foundation & Tooling)
โครงสร้างและการจัดการหน้าเว็บ (Routing & Structure)
ส่วนประกอบและการจัดการโค้ด (Code Organization)
ไฟล์ตั้งค่า (Configuration Files)
การจัดการ Dependencies Version


1. เครื่องมือพื้นฐาน (Foundation & Tooling)
1.1 React Component

เรียกว่า: Component (คอมโพเนนต์)
ใช้ทำอะไร: สร้างชิ้นส่วน UI ที่นำกลับมาใช้ซ้ำได้ เช่น Button, Card, Navbar - เป็นหัวใจของการสร้างหน้าตาเว็บทั้งหมด
เป็นของ: React (R) 🎨
ตัวอย่าง: <ProductCard />, <Button />, <Navbar />

1.2 JSX (JavaScript XML)

เรียกว่า: JSX - JavaScript Syntax Extension
ใช้ทำอะไร: ไวยากรณ์พิเศษที่ทำให้เขียนโค้ดหน้าตาคล้าย HTML ในไฟล์ JavaScript ได้โดยตรง ทำให้เห็นภาพ UI ได้ชัดเจนขณะเขียนโค้ด
เป็นของ: React (R) 🎨
ตัวอย่าง: <div className="card">{title}</div>

1.3 package.json

เรียกว่า: Project Manifest (ไฟล์ประจำตัวโปรเจค)
ใช้ทำอะไร:

เก็บข้อมูลโปรเจค (ชื่อ, เวอร์ชัน, คำอธิบาย)
รายการ Dependencies (เครื่องมือที่ต้องติดตั้ง)
คำสั่ง Scripts สำหรับรัน, build, test โปรเจค


เป็นของ: Node.js (มาตรฐาน - ทั้ง React และ Next.js ใช้)
สำคัญ: เป็นจุดเริ่มต้นที่ทุกคนในทีมต้องเข้าใจตรงกัน

1.4 node_modules

เรียกว่า: Dependencies Storage (โกดังเก็บเครื่องมือ)
ใช้ทำอะไร: เก็บโค้ดของ library และ package ทั้งหมดที่ติดตั้งตามรายการใน package.json
เป็นของ: Node.js (มาตรฐาน)
หมายเหตุ:

ขนาดใหญ่มาก (มักหลักร้อย MB)
ไม่ควร commit ลง Git (ใช้ .gitignore)
สร้างใหม่ได้ด้วยคำสั่ง npm install




2. โครงสร้างและการจัดการหน้าเว็บ (Routing & Structure)
2.1 /app Directory & File-based Routing

เรียกว่า: App Router (ระบบ Routing อัตโนมัติ)
ใช้ทำอะไร: สร้างหน้าเว็บและ URL จากโครงสร้างโฟลเดอร์โดยอัตโนมัติ ไม่ต้องเขียน routing config เอง
เป็นของ: Next.js (N) 🗺️
ตัวอย่าง:

app/page.jsx → /
app/products/page.jsx → /products
app/products/[id]/page.jsx → /products/123



2.2 layout.jsx

เรียกว่า: Layout Component (โครงหลัก)
ใช้ทำอะไร: เป็นไฟล์พิเศษที่ทำหน้าที่เป็น "แม่แบบ" wrap ทุกหน้าในระดับเดียวกัน - ใช้สำหรับ Navbar, Footer, Sidebar ที่ต้องแสดงทุกหน้า
เป็นของ: Next.js (N) 🗺️
กฎสำคัญ:

Root Layout (app/layout.jsx) ต้องมี <html> และ <body>
Layout ลูกทุกตัว ห้าม มี <html> และ <body> (ใช้ <div> แทน)
Layout ทุกตัวต้องรับและ render {children}



2.3 page.jsx

เรียกว่า: Page Component (หน้าเว็บ)
ใช้ทำอะไร: กำหนดเนื้อหาหลักของแต่ละหน้า - เป็น "ตัวจริง" ของ URL นั้นๆ
เป็นของ: Next.js (N) 🗺️
กฎสำคัญ:

ห้าม มี <html> หรือ <body>
ใช้ <main>, <div>, <section> เป็น wrapper
ต้อง export default function



2.4 Special Files (not-found.jsx, error.jsx, loading.jsx)

เรียกว่า: Special Route Files (ไฟล์จัดการสถานะพิเศษ)
ใช้ทำอะไร:

not-found.jsx - หน้า 404 เมื่อไม่พบ route
error.jsx - จัดการ error boundary
loading.jsx - แสดง UI ระหว่างโหลดข้อมูล (Suspense)


เป็นของ: Next.js (N) 🗺️
ประโยชน์: ทำให้จัดการ UX ในกรณีพิเศษได้อย่างเป็นระบบ

2.5 /api Directory (API Routes)

เรียกว่า: API Routes (Backend ใน Frontend)
ใช้ทำอะไร: สร้าง Backend API endpoints ภายในโปรเจกต์ Next.js ได้เลย - ทำให้ไม่ต้องสร้างเซิร์ฟเวอร์แยก
เป็นของ: Next.js (N) 🗺️
ตัวอย่าง: app/api/products/route.js → /api/products
ข้อดี: Fullstack ในที่เดียว, ใช้ร่วมกับ Database ได้ง่าย


3. ส่วนประกอบและการจัดการโค้ด (Code Organization)
3.1 /components Directory

เรียกว่า: UI Components Library (คลังชิ้นส่วน UI)
ใช้ทำอะไร: จัดเก็บ React Components ที่นำกลับมาใช้ซ้ำได้ ทำให้ง่ายต่อการค้นหาและบำรุงรักษา
เป็นของ: Convention (ข้อตกลงของทั้ง React และ Next.js)
โครงสร้างแนะนำ:

